[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"
extend-exclude = [".beads", ".codex", ".ntm", ".pytest_cache"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F"]
